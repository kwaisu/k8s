apiVersion: v1
kind: Service
metadata:
   name: grafana-nodeport
   namespace: istio-system
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30101
  selector:
    app: grafana 
